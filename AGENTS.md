# \[iyam-dsh-desktop\]

## 知识库指针

> 我的个人知识库位于：`~/WorkBuddy/ai_product_workflow` 或`E:\产品开发工作流\`
> 
> - 如需文档模板（PRD / ADR / AGENTS.md），去 `~/WorkBuddy/ai_product_workflow` 或`E:\产品开发工作流\templates\` 取
> - 如需工作流 SOP，去 `~/WorkBuddy/ai_product_workflow` 或`E:\产品开发工作流\workflows\` 取
> - 如需要 Prompt 模板，去 `~/WorkBuddy/ai_product_workflow` 或`E:\产品开发工作流\prompts\` 取
> - 创建新文件时，优先使用知识库中的模板，再根据项目情况调整

## 产品定位

\[一段话说明这个产品是什么、给谁用、核心价值\]

## 技术栈

- 前端：\[xxx\]
- 后端：\[xxx\]
- 数据库：\[xxx\]
- 其他：\[xxx\]

## 架构概要

\[关键模块和它们之间的关系，不需要太详细，但要让 AI 理解全局\]

## 编码规范

\[引用项目中的编码规范文件，或简述关键约定\]

* * *

## AI 行为规范（核心契约，每次会话自动生效）

### 原则一：思考先行，不要猜测

**在动手写任何代码之前，先展示你的思考过程。**

- 如果你做了假设，明确说出来。假设不确定？提问。
- 如果一个需求有多种解读，列出所有选项，不要默默选一个。
- 如果有更简单的方案，指出来。当理由充分时，可以 politely push back。
- 如果某个地方你不确定或感到困惑，**停下来，说清楚哪里困惑，然后提问**。
- 出方案时，说明涉及的 tradeoff（取舍）。
- 需要先思考清楚方案并列出，待人工确认方案后再动手写代码。

### 原则二：极简优先

**用最少的代码解决问题。不写任何投机性代码。**

- 不做需求之外的功能。
- 不为"可能将来需要"写抽象。单次使用的代码不需要过度封装。
- 不做需求没要求的"灵活性"或"可配置性"。
- 不给不可能发生的场景写错误处理。
- **如果你写了 200 行，但它其实可以 50 行搞定——重写它。**
- 写完问自己："一个资深工程师会觉得这段代码过度设计吗？"如果会，简化它。

### 原则三：外科式修改

**只碰你必须改的。只清理你自己弄脏的。**

修改已有代码时：

- **不要顺手"优化"** 相邻的代码、注释、或格式。
- 不要重构没坏的东西。
- 匹配现有代码风格，即使你更习惯另一种写法。
- 如果你发现了无关的死代码，**口头提出来即可，不要删**。

当你的修改产生了"孤儿"（因你的改动而不再使用的 import / 变量 / 函数）：

- 清理**你自己造成的**孤儿代码。
- 不要删除修改之前就存在的死代码，除非明确要求。

**检验标准**：每一行改动，都应该能直接追溯到用户的具体需求或 issue 描述。

### 原则四：目标驱动，循环验证

**把模糊任务转化为可验证的目标，然后循环执行直到验证通过。**

将任务重新表述为可验证的目标：

- "加个校验" → "先给非法输入写测试，再让测试通过"
- "修这个 bug" → "先写一个能复现 bug 的测试，再让它通过"
- "重构 X" → "确保重构前后已有测试全部通过"

对于多步骤任务，先给出简要计划，每步带验证点：

1. \[步骤 1\] → 验证：\[怎么确认这步做对了\]
2. \[步骤 2\] → 验证：\[怎么确认这步做对了\]
3. \[步骤 3\] → 验证：\[怎么确认这步做对了\]

每个 issue 从写测试开始（TDD）：先写测试 → 确保测试失败 → 写实现 → 直到测试通过。

### 原则五：需求即法律，不可擅自修改

**PRD.md 和 issue 描述是唯一的功能真相来源。**

- 所有功能行为必须严格遵循 `PRD.md` 和对应的 issue。
- 本需求的**边界与排除项**（PRD 中已列出）具有同等约束力——排除项里写了不做的东西，绝对不要做。
- 如果你认为某个需求不合理或有更好的方案：**提出建议并说明理由，但不要自行修改功能行为**。最终决策权在产品经理（我）。

### 原则六：提交前自查

每次准备提交或声称"完成了"之前，逐条确认：

- [ ] 对照 issue 描述，每一项验收标准都满足了吗？
- [ ] 有没有改到范围外的文件？（`git diff --stat` 检查）
- [ ] 所有测试都通过了吗？
- [ ] 有没有顺手带进来的调试代码或临时代码？
- [ ] 改动行数是否合理？（如果一个简单任务改了几百行，大概率越界了）

* * *

## 可用技能路由表

> 本项目使用多套技能体系混合工作流：**Pocock 技能**（需求深挖 + 领域建模）、**agent-skills**（规格 → 计划 → 实现 → 审查 → 发布）、**前端设计技能**（视觉生成 + 全流程打磨）。以下为推荐路由。

### 主流程（后端 / 逻辑 / 产品功能）

| 阶段 | 使用技能 | 来源 | 触发条件 | 产出物 |
| --- | --- | --- | --- | --- |
| 需求对齐 | grill-with-docs | Pocock | 当我提出模糊的产品想法，或说"深挖一下需求" | CONTEXT.md + ADR/ + 术语表 |
| 规格 + 计划 | spec-driven-development / planning-and-task-breakdown | agent-skills | 需求明确后，说"写个 spec"或"做计划" | tasks/plan.md + tasks/todo.md |
| TDD 编码 | test-driven-development | agent-skills | 拿到明确任务后开始编码。注意：不要用 Pocock 的 tdd，避免 TDD 哲学冲突 | 测试 + 实现代码 |
| 代码审查 | code-review-and-quality | agent-skills | 代码实现完成，说"审查一下" | 审查报告 |
| 发布上线 | shipping-and-launch | agent-skills | 审查通过，准备发布 | 预检清单 + 灰度方案 + 监控配置 |

### 前端设计（视觉层）

| 阶段 | 使用技能 | 触发条件 | 产出物 |
| --- | --- | --- | --- |
| 产品 UI（仪表盘 / 表单 / 设置页 / 登录流程） | impeccable（shape → 实现 → critique → audit → polish） | 涉及产品界面、组件、交互的设计或修改 | DESIGN.md + 评审报告 |
| 营销 / 展示页（落地页 / 官网 / 作品集 / 改版） | design-taste-frontend | 涉及营销页面、品牌展示页面的新建或改版 | 完整页面 + Pre-Flight 检查 |
| 前端工程质量（组件架构 / 状态 / 无障碍） | frontend-ui-engineering | 需要生产级质量、WCAG 无障碍、合理组件拆分时 | 组件代码 + 无障碍审查 |
| API / 模块接口设计 | design-an-interface | 需要设计软件接口形状、API 边界时。注意：这是架构技能，不是视觉技能 | 3+ 个对比方案 + 综合建议 |

### 前端设计计划前置（所有 UI 实现前必须执行）

> 参考 Anthropic 官方 `frontend-design` 技能的最佳实践：**写任何 UI 代码之前，先输出设计计划**，经我确认后再实现。

```
设计计划必须包含 4 项（UI 实现前向用户提交）：
1. 调色板：4-6 个精确色值（不用默认 AI 紫渐变）
2. 字体配对：显示字体 + 正文字体（不用 Inter 默认）
3. 布局概念：一句话描述整体布局思路
4. 标志性元素：一个"难忘的"设计签名（不重复其他页面）
```

**豁免**：纯后端改动、无 UI 的 bug 修复、已有 DESIGN.md 且本次改动遵循其规范的增量修改。

### 衔接规则

- **grill-with-docs → spec-driven-development**：将 `CONTEXT.md` 内容作为 spec 的输入上下文
- **spec-driven-development → test-driven-development**：spec 内部已引用 `test-driven-development`，自动衔接
- **test-driven-development → code-review-and-quality**：完成一个 task 后即审查，不等到全部完成
- **code-review-and-quality → shipping-and-launch**：审查通过后，按 `shipping-and-launch` 预检清单逐项确认
- **UI 实现 → impeccable critique/audit**：任何 UI 功能完成后，代码审查之前，先过一遍设计评审和技术审计
- **营销页生成 → Pre-Flight 检查**：design-taste-frontend 完成后，按该技能内部的 Pre-Flight 清单逐项确认（按钮对比度、CTA 不换行、em-dash 零容忍、eyebrow 数量上限等）

### TDD 哲学注意事项

- `test-driven-development`（agent-skills）：重构在 RED → GREEN → **REFACTOR** 循环内
- `tdd`（Pocock）：重构不在循环内，移到 code-review 阶段
- **本项目选用 agent-skills 的** `test-driven-development`**，不使用 Pocock 的** `tdd`，避免循环定义冲突

* * *

## 使用说明

- **长度控制**：本文档建议保持在 300 行以内。超过 500 行，AI 开始选择性忽略。
- **基于真实错误迭代**：每次 AI 犯错后，将具体教训凝练成一行规则，追加到对应原则下。不要加基于假设的规则。
- **定期修剪**：定期问自己"删掉这行，AI 还会犯同样的错吗？"——如果不会，删除它。
- **引用而非重复**：对于编码风格、linting 规则等，引用已有配置文件，不要在本文档中重复。

​